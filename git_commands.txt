# git init
# git add .
# git commit -m "Your message goes here"
# git status
# git log (hit "q" to quit)
# git checkout (copy commit id from kraken )

Git Remote Commands.....
# git remote add origin(url)
# git remote -v
# git push origin master
# git pull origin master
